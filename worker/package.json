{
  "name": "prism-short-worker",
  "version": "1.0.0",
  "private": true,
  "description": "JSON Prism short-link service — POST /s to store, GET /<slug> to redirect.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "typescript": "^5.8.3",
    "wrangler": "^3.100.0"
  }
}
