# Local frontend + local short-link Worker (separate terminal, port 8787).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SHORTENER_URL=http://localhost:8787
# Leave donations disabled locally; only configure a test checkout for staging.
NEXT_PUBLIC_DONATE_URL=
